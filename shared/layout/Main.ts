import styled from '@emotion/styled';

const Main = styled.main({
    padding: '1rem 2rem',
    display: 'flex',
    flexDirection: 'column',
    flex: 1,
    maxWidth: '100vw',
    minWidth: 'min(42rem, 100vw)',
});

export default Main;
